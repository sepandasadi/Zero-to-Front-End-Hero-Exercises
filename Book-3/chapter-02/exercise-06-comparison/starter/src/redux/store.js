import { configureStore } from '@reduxjs/toolkit'
// TODO: Import counterReducer from './counterSlice'

export const store = configureStore({
  reducer: {
    // TODO: Add counter: counterReducer
  },
})

