{
  "name": "sass-design-tokens",
  "version": "1.0.0",
  "description": "Exercise 1: Design Tokens with Maps",
  "scripts": {
    "watch": "sass --watch scss:css --style expanded",
    "build": "sass scss:css --style compressed"
  },
  "devDependencies": {
    "sass": "^1.69.0"
  }
}

