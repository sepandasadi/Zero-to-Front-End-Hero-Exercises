# Exercise 4: Semantic HTML5 Layout

## 🎯 Objective

Build a complete blog post page using proper HTML5 semantic elements. Practice using `<header>`, `<nav>`, `<main>`, `<article>`, `<section>`, `<aside>`, and `<footer>`.

## 📋 Requirements

Create a blog post page titled "The Future of Web Development" with the following structure:

**Page `<header>`:**
- Site title/logo
- Navigation menu (`<nav>`) with links: Home, About, Blog, Contact

**`<main>` content area containing:**

**`<article>` - The blog post:**
- Article `<header>` with:
  - Post title (h1)
  - Author name
  - Publication date (`<time>` element)
- Multiple `<section>`s for different parts of the post:
  - Introduction
  - Current Trends
  - Future Predictions
  - Conclusion
- Each section should have an h2 heading

**`<aside>` - Sidebar with:**
- "About the Author" section
- "Related Posts" list
- "Popular Tags"

**Page `<footer>`:**
- Copyright notice
- Contact information
- Social media links

## ✅ Requirements

- ✅ Proper HTML5 semantic structure
- ✅ One `<h1>` for the post title
- ✅ `<time>` element with `datetime` attribute
- ✅ All major semantic elements used appropriately
- ✅ Clear hierarchy and organization
- ✅ Valid HTML

## 💡 Tips

1. Think about the page as containers of meaning
2. Use `<article>` for the main content
3. Use `<aside>` for tangentially related content
4. `<section>` groups related content within the article
5. Each section should have a heading

## 📁 Files

- **Starter:** `starter/blog-post.html`
- **Solution:** `solution/blog-post.html`

**Build a semantic blog post!** 📰

