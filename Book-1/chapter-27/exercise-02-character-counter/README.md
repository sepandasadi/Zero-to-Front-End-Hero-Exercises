# Exercise 2: Live Character Counter ⭐⭐

## 🎯 Objective

Create a textarea with a live character counter (like Twitter).

## 📋 Requirements

- A textarea with a maximum length of 280 characters
- A counter showing "280 characters remaining"
- Counter updates in real-time as the user types
- Counter turns red when user exceeds the limit

## 🎁 Bonus Challenges

- Disable a submit button when over the limit
- Show a percentage indicator or progress bar
- Add different color stages (green → yellow → orange → red)

## 💡 Hints

- Use the `input` event to listen for changes
- Check `textarea.value.length` to get the current character count
- Update the counter text with the remaining characters
- Use conditional logic to change colors based on remaining count
- Disable a button with `button.disabled = true`

## 🚀 Getting Started

1. Open `index.html` in your browser
2. Read the instructions on the page
3. Write your code in the designated areas
4. Test by typing in the textarea

## ✅ Success Criteria

- Counter shows correct remaining characters
- Updates in real-time as you type
- Turns red when over 280 characters
- Submit button disabled when over limit (bonus)
- Multiple color stages based on count (bonus)

## 📚 Concepts Practiced

- Input events
- Real-time validation
- Conditional styling
- User feedback
- Form controls

## ⏱️ Estimated Time

20-30 minutes

---

**Good luck!** Remember, the solution is at the bottom of `index.html` if you get stuck.

