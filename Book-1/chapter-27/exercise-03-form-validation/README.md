# Exercise 3: Form with Validation ⭐⭐⭐

## 🎯 Objective

Create a signup form with real-time validation.

## 📋 Requirements

- Username field (minimum 3 characters)
- Email field (must contain @ and .)
- Password field (minimum 8 characters)
- Show error messages below invalid fields
- Submit button should be disabled until all fields are valid
- Prevent form submission and handle it with JavaScript

## 🎁 Bonus Challenges

- Add password strength indicator (weak/medium/strong)
- Show green checkmarks next to valid fields
- Add a "Show Password" toggle button
- Add a "Confirm Password" field that must match the password

## 💡 Hints

- Use the `input` event to validate in real-time
- Use `e.preventDefault()` on the submit event
- Add/remove CSS classes like `.valid` and `.invalid`
- Check `input.value.length` for length validation
- Use `includes()` to check for @ and . in email

## 🚀 Getting Started

1. Open `index.html` in your browser
2. Read the instructions on the page
3. Write your code in the designated areas
4. Test by typing in each field

## ✅ Success Criteria

- All validation rules work correctly
- Error messages appear for invalid fields
- Submit button is disabled until all fields are valid
- Form prevents default submission
- Success message appears when form is valid

## 📚 Concepts Practiced

- Form events
- Real-time validation
- User feedback
- Conditional logic
- DOM manipulation

## ⏱️ Estimated Time

30-45 minutes

---

**Good luck!** Remember, the solution is at the bottom of `index.html` if you get stuck.

