# Chapter 13 — Testing Fundamentals (Companion Files)

These files are framework-agnostic where possible and assume **Vitest + React Testing Library** for unit/integration and **Cypress** for E2E examples.

## Suggested install (optional)
```
npm i -D vitest @testing-library/react @testing-library/user-event @testing-library/jest-dom jsdom
npm i -D cypress
```
