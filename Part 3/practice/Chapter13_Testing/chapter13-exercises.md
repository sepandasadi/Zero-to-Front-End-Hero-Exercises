# Chapter 13 — Testing Fundamentals (Exercises)

1) **Unit**: Write Vitest tests for `isEmail` covering valid, invalid, and trimmed inputs.  
2) **Integration**: Mock `fetch` in `fetchItems` to cover 200 JSON and 500 error.  
3) **Component**: Create a small `<LoaderList />` that calls `fetchItems` on mount and shows `loading` → `items` → `error`. Test all states with RTL.  
4) **E2E (Optional)**: Add a Cypress spec that visits `/counter`, increments once, and asserts the visible count.
