# Exercise 2: Text Formatting

## 🎯 Objective

Practice using semantic text formatting tags to markup various types of content. Learn when to use `<strong>`, `<em>`, `<mark>`, `<code>`, `<sub>`, `<sup>`, and other formatting elements.

## 📋 Requirements

Create a page that demonstrates proper use of text formatting tags. Include:

**1. Important Information**
- Use `<strong>` for text that has strong importance
- Example: "**Warning:** This action cannot be undone."

**2. Emphasized Text**
- Use `<em>` for text that needs emphasis
- Example: "You *must* complete this step first."

**3. Highlighted Text**
- Use `<mark>` for text that's highlighted for reference
- Example: Search results where the search term is highlighted

**4. Code Examples**
- Use `<code>` for inline code
- Use `<pre>` for code blocks
- Example: "Use the `console.log()` function to debug."

**5. Abbreviations**
- Use `<abbr>` with `title` attribute
- Example: HTML, CSS, API

**6. Citations and Quotes**
- Use `<cite>` for work titles
- Use `<q>` for short inline quotes
- Use `<blockquote>` for longer quotes

**7. Subscript and Superscript**
- Use `<sub>` for subscripts (H₂O)
- Use `<sup>` for superscripts (x²)

**8. Deleted and Inserted Text**
- Use `<del>` for deleted text
- Use `<ins>` for inserted text

## ✅ Requirements

- ✅ At least one example of each tag mentioned
- ✅ Proper semantic usage (not just for styling)
- ✅ Clear examples with context
- ✅ Well-organized with headings

## 💡 Tips

1. Choose tags based on meaning, not appearance
2. Use `<strong>` for importance, not just bold text
3. Use `<em>` for emphasis, not just italics
4. Every formatting tag should have semantic purpose

## 📁 Files

- **Starter:** `starter/formatting.html`
- **Solution:** `solution/formatting.html`

**Start practicing semantic text formatting!** ✍️

