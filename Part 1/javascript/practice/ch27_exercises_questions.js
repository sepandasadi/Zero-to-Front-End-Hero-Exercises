// Chapter 27 Exercises: Events and Interactivity (Questions Only)

// Exercise 1: Button Click
// Create a button with id="logBtn". Add a click listener that logs "Clicked!" each time it is pressed.

// Exercise 2: Toggle Visibility
// Add a button with id="toggleBtn" and a paragraph with id="para".
// When the button is clicked, toggle a "hidden" class on the paragraph.

// Exercise 3: Form Handling
// Given a form with id="userForm" and two inputs with ids "name" and "email":
// 1) Prevent the default submission.
// 2) Read the values of the two inputs and log them.
// 3) If name is empty, log "Name is required".

// Exercise 4: Event Delegation
// Given a <ul id="colorList"> containing several <li> elements:
// Add one click listener on the <ul> that changes the clicked <li>'s text color to "tomato".
// Use event.target or closest to ensure you handle clicks on the <li> only.

// Exercise 5: Dynamic List
// Given a button id="addItem" and a <ul id="items">:
// Add a click handler that creates a new <li> with text "New item" and appends it to the list.

// Exercise 6: Key Detection
// Given an input with id="search":
// Add a keyup listener that logs the pressed key (e.key).

// Exercise 7: Remove Listener
// Add a button id="onceBtn" that logs "Once" only on the first click, then removes its listener.
