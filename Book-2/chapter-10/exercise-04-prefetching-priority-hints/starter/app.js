// TODO: Implement prefetch on hover

// Step 1: Select all post links
// const postLinks = document.querySelectorAll('.post-link');

// Step 2: Add mouseenter event listener
// postLinks.forEach(link => {
//   link.addEventListener('mouseenter', () => {
//     // Create prefetch link
//     const prefetchLink = document.createElement('link');
//     prefetchLink.rel = 'prefetch';
//     prefetchLink.href = link.href;
//     document.head.appendChild(prefetchLink);
//
//     console.log('Prefetched:', link.href);
//   }, { once: true }); // Only prefetch once
// });

// TODO: Measure performance
// const start = performance.now();
// // ... measure load time

console.log('App loaded. Implement prefetch on hover!');

