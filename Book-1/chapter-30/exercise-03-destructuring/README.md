# Exercise 3: Destructuring

## 🎯 Objective
Master array and object destructuring for cleaner code.

## 📝 Instructions

1. **Array Destructuring:**
   - Extract first, second, and rest from an array of numbers
   - Skip elements when destructuring

2. **Object Destructuring:**
   - Extract user properties with renaming
   - Use default values for missing properties
   - Destructure nested objects

3. **Function Parameters:**
   - Create a function that destructures an object parameter
   - Use default values in parameters

4. **API Response:**
   - Destructure a complex API response
   - Extract only needed fields

## ✅ Success Criteria

- All examples use destructuring syntax
- Nested destructuring works correctly
- Default values are used appropriately
- Function parameters are destructured

## 💡 Hints

- `const [a, b, ...rest] = array`
- `const { name, age = 18 } = object`
- `function greet({ name, age }) { ... }`

