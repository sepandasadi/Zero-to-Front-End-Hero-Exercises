# Exercise 6: Map & Set

## 🎯 Objective
Use Map and Set data structures effectively.

## 📝 Instructions

1. **Map Operations:**
   - Create a Map to store user roles
   - Add, get, check, and delete entries
   - Iterate over Map entries

2. **Set Operations:**
   - Create a Set to track unique visitors
   - Add items (duplicates auto-ignored)
   - Remove duplicates from an array

3. **Practical Applications:**
   - Build a cache system with Map
   - Track unique tags with Set
   - Combine both for complex data structures

## ✅ Success Criteria

- Map correctly stores any type of key
- Set automatically removes duplicates
- Can iterate over both structures
- Understand when to use each

## 💡 Hints

- `new Map()` and `new Set()`
- `map.set(key, value)`, `map.get(key)`, `map.has(key)`
- `set.add(value)`, `set.has(value)`, `set.delete(value)`
- `[...new Set(array)]` removes duplicates

