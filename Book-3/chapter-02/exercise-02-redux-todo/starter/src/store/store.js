import { configureStore } from '@reduxjs/toolkit'
// TODO: Import todoReducer from todoSlice

// TODO: Configure and export the store
export const store = configureStore({
  reducer: {
    // TODO: Add todos reducer
  },
})

